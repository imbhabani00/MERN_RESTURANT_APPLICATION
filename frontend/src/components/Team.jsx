import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
            At Foodies Delight, our success is driven by the passion and
            dedication of our talented team. From chefs and developers to
            designers and marketers, each member plays a vital role in creating
            an exceptional culinary experience for our community. Get to know
            the faces behind the flavors and discover the people who make
            Foodies Delight a hub of culinary inspiration and innovation.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
